import { makeStyles } from '@material-ui/styles';


const useStyles = makeStyles((theme) => ({

      gridContainer: {
        color: "white",
      },
      
      socialMediaIcon: {
        margin: "2px 10px",
      },
      socialMediaIconsDiv: {
        marginLeft: "20px",
        display: "inline"
      },
      socialMedia: {
        margin: "5%"
      }
      

}));

export default useStyles;