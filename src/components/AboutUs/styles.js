import {makeStyles} from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
    aboutUs: {
        margin: "10% 20%",
      },
      aboutUsTitle: { 
        margin: "50px 0px",
      },
      aboutUsContent: { 
        margin: "50px 0px",
      }
}));

export default useStyles;