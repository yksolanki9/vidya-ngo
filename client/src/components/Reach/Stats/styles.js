import { makeStyles } from '@material-ui/styles';


const useStyles = makeStyles((theme) => ({
      statCards: {

      },
      gridContainer: {
        position: "relative",
        // margin: "0% 10%",
        overflow: "hidden",
        color: "white",
      }
    }));

export default useStyles;